
  # Design Bridge UI

  This is a code bundle for Design Bridge UI. The original project is available at https://www.figma.com/design/yZxQbq8HkvtNpOAzHrbYFV/Design-Bridge-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # Bridge
